# Copy this file to .env and fill in real values. .env is gitignored.
#
# GITHUB_TOKEN — optional. Gets you 5000 requests/hour instead of 60 (unauth).
#                Used by pipelines/ingest_github.py.
GITHUB_TOKEN=

# Add new source credentials below, one per line, with a comment naming the pipeline.
